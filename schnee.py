"""import turtle as t
from random import uniform

t.speed(0)
t.bgcolor('black')
t.pensize(5)

def art():
    for i in range(3):
        
        for i in range(3):
            t.forward(30)
            t.backward(30)
            t.right(45)
        t.left(90)
        t.backward(30)
        t.left(45)
    t.right(90)
    t.forward(90)
for i in range(8):
    t.pencolor(uniform(0,1),uniform(0,1),uniform(0,1))
    art()
    t.left(45)
t.done()"""